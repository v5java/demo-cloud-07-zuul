spring:
  application:
    name: xuanyu-prop
  cloud:
    config:
      profile: prod
      label: master
      discovery:
        enabled: true
        serviceId: xuanyu-config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1100/eureka/